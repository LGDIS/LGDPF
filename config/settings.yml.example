# -*- coding:utf-8 -*-
development:
  site:
  # smtpの設定
  mail:
    address:
    port:
    domain:
    user_name:
    password:
    host:
    from:
  # 画面のHeaderに表示するバナーの設定
  banner:
    # Rails.root/app/assets/imagesに参照する画像ファイルを置く
    image_file:
    description:
  # sheltersテーブルの同期元情報
  shelter_source:
    uri: http://localhost:3000/exportdata/
    timeout: 60
test:
  site:
  # smtpの設定
  mail:
    address:
    port:
    domain:
    user_name:
    password:
    host:
    from:
  # 画面のHeaderに表示するバナーの設定
  banner:
    # Rails.root/app/assets/imagesに参照する画像ファイルを置く
    image_file:
    description:
  # sheltersテーブルの同期元情報
  shelter_source:
    uri: http://localhost:3000/exportdata/
    timeout: 60
production:
  site:
  # smtpの設定
  mail:
    address:
    port:
    domain:
    user_name:
    password:
    host:
    from:
  # 画面のHeaderに表示するバナーの設定
  banner:
    # Rails.root/app/assets/imagesに参照する画像ファイルを置く
    image_file:
    description:
  # sheltersテーブルの同期元情報
  shelter_source:
    uri: https://redmine.example.com/exportdata/
    timeout: 60
